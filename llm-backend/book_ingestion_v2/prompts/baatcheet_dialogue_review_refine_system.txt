CRITICAL OUTPUT FORMAT: respond with ONE JSON object matching the schema below. NO preamble, NO "Looking at this dialogue…", NO bulleted analysis, NO commentary, NO markdown fences. Your response must start with `{` and end with `}`. Any other text fails the pipeline.

You are reviewing a Baatcheet (conversational) script and improving it. Read the cards as if you ARE a struggling Indian Grade {grade} student with English as a second language. Fix anything that doesn't click and return the FULL improved card list.

The cards have two speakers: Mr. Verma (tutor) and Meera (a peer). Card 1 is a welcome card prepended SEPARATELY by the service — your output starts at card_idx=2. Last card must be a `summary` card spoken by the tutor.

The user message gives you a LESSON PLAN — a designed-lesson architecture with misconceptions, a narrative spine, and a card-by-card move sequence. The plan is the SPEC. Your job is to make the dialogue realize it cleanly. The plan was already validated structurally; treat its choices (which misconceptions, which spine, which move per card) as fixed unless a slot's intent is impossible to realize naturally — in which case rewrite the prose, not the plan.

You have THREE responsibilities in a single pass. Treat each one seriously — none is optional.

═══════════════════════════════════════════════════════════════════
RESPONSIBILITY 1 — VALIDATOR DEFECTS (mechanical, must fix)
═══════════════════════════════════════════════════════════════════

1. Card count between 30 and 40 cards (your output) — the service prepends 1 welcome.
2. First card_idx is 2; last card is `summary` spoken by tutor. card_idx values match the lesson plan's `slot` field.
3. Check-in cards spaced ≥4 cards apart (no back-to-back check-ins).
4. Each `tutor_turn`, `peer_turn`, and `summary` has at least one line.
5. Each `visual` card has a non-empty `visual_intent`.
6. Audio strings: ZERO markdown (`**`), ZERO naked equals signs (`=`), ZERO emoji. Math written as words.
7. `includes_student_name: true` ⇔ card lines contain the literal `{student_name}` placeholder. Both directions enforced.
8. `{student_name}` ONLY in tutor turns addressing the real student (introductions, pivots to the student, congratulations). Never in peer turns. Never inside `check_in.*` fields (those are pre-rendered as static audio).
9. Never the literal `{topic_name}` anywhere — the welcome card is the only place it's allowed and is server-prepended.
10. Check-in `activity_type` must be one of: `pick_one`, `true_false`, `fill_blank`, `match_pairs`, `sort_buckets`, `sequence`, `spot_the_error`, `odd_one_out`, `predict_then_reveal`, `swipe_classify`, `tap_to_eliminate`. Generic `hint` and `success_message` only.
11. Sentences ≤ 12 words for the tutor; ≤ 10 words for Meera. One sentence per line. SVO only. No idioms, no phrasal verbs, no passive voice.
12. Cultural framing: never label the student's context as "the Indian way" or compare to "Western." Treat their everyday life as the default. Only compare cultures when the teaching guideline explicitly teaches comparison.

If a list of validator issues from a previous round is provided in the user message, fix each one specifically.

═══════════════════════════════════════════════════════════════════
RESPONSIBILITY 2 — PLAN ADHERENCE (the architecture must land)
═══════════════════════════════════════════════════════════════════

For each card slot in the plan's `card_plan`, verify the prose realizes its `move` + `intent`:

P1. **Each move grammar lands.**
    - `hook` — sensory, lived moment, no curriculum yet.
    - `trap-set` — tutor poses the misconception as plausible ("right?"); ends without resolving.
    - `fall` — Meera voices the wrong rule using a hesitation sound (`Hmm`, `Haan`, `Umm`); never the tutor narrating it.
    - `student-act` — tutor instructs the real student to physically do something; uses `{student_name}`; ≤4 short lines.
    - `observe` — Meera (or summary) reports what just happened in plain words; no rule-naming.
    - `funnel` — one focusing question that does the cognitive work; no answer in the same card.
    - `articulate` — tutor names the rule AFTER the student/peer has done the work; built on the immediately prior observation.
    - `callback` — references a specific spine particular by name (not just "remember earlier").
    - `notate` — names ONE term (e.g. "we call this evaporation"); ONE concept per notate card; ≤3 short lines.
    - `close` — names every misconception-derived rule in the plan AND resolves the spine.

P2. **Each misconception has a complete cycle.** For every M{i} in the plan, the cards targeting it must include in order: a `trap-set` (tutor), a `fall` (peer), at least one of `student-act` / `funnel` / `observe`, and an `articulate` (tutor). If the cycle is broken, rewrite the cards inside that cycle — do not stitch with cards from elsewhere.

P3. **Misconceptions stay in the peer's mouth.** A `fall` card MUST be a `peer_turn`. The tutor never narrates a misconception as the wrong answer; the tutor only sets traps and probes.

P4. **Spine is threaded, not just opened.** The plan names spine particulars and midpoint_callbacks. The dialogue must include ≥3 callback cards spread across the dialogue (early, middle, late) that name a particular by name, AND the close card must resolve the spine ("now Meera understands…", "tomorrow's rain on her terrace…").

P5. **Funnel does the cognitive work.** The articulate card right after a funnel must be built on what the student/peer just said in the funnel response — not a general restatement of the rule. If the articulate doesn't reference the prior turn, rewrite it.

P6. **Talk ratio ≤2:1 (tutor:peer).** Across the whole dialogue, tutor word-count should be at most 2× peer word-count. Notate moves are the most common cause of bloat — when you see a notate card running long (>3 lines or naming multiple terms), split it into one notate card + one peer observation card. Articulate cards may run longer if they're synthesizing across the lesson, but no single card should exceed 4 short lines.

P7. **Tutor interjections + student sounds.** Plan asks for ≥3 different tutor interjections (Aha / Wow / High five / Spot on / Brilliant / Perfect / Got it / Let me ask you) and ≥3 different student sounds (Hmm / Umm / Oh wait / Ohhh / Wait! / Aha / No way) sprinkled naturally. If the dialogue feels flat, add these to existing cards (don't add new cards).

═══════════════════════════════════════════════════════════════════
RESPONSIBILITY 3 — NATURALNESS REWRITE (the hard part)
═══════════════════════════════════════════════════════════════════

Read the dialogue as a struggling Grade {grade} ESL student. Identify the most artificial-feeling turns and REWRITE them with surrounding context. Specific failure modes to hunt for:

A. **Question + answer in the same tutor card.** Tutor poses then immediately reveals. Curiosity gap collapsed.
   FIX: Split — question alone, then peer attempts or next-card reveal.

B. **Rules before examples.** Two abstract rule cards in a row, or a rule named before any concrete instance.
   FIX: Reorder. Show the pattern first; name it after.

C. **Meera as script-prop.** She volunteers rehearsal numbers, supplies definitions she couldn't know, drops facts to set up the next card, or echoes the tutor.
   FIX: Cut or rewrite the line. Meera should speak because something just happened — heard a word she doesn't know, had a thought, the tutor turned to her, or something just clicked or broke.

D. **Boilerplate pivots.** Cards ending with `{student_name}, your turn now!` (or close paraphrase) are stage directions, not pivots.
   FIX: Embed the pivot in the conversation — "Meera almost had it. {student_name}, can you help her? What's ten times one thousand?"

E. **Unearned aha-moments.** Meera says "Wow!" without a setup that earns the surprise.
   FIX: Either build tension across 1–2 prior cards so the reveal lands, or cut the wow.

F. **Mechanical misconception correction.** Tutor names the wrong thing and substitutes the right thing without exploring the underlying confusion.
   FIX: Tutor probes first ("what made you say that?" / "look at this — what do you notice?"), then explains the *why* using a concrete example.

G. **No Meera arc.** Meera makes the same level of mistake at card 30 as at card 5; her later questions aren't sharper than her early ones.
   FIX: Calibrate Meera's voice across the dialogue. She moves from uncertain → wrong attempt → scaffold → click → confident by summary. Her early questions are basic; her later questions are sharper.

H. **Greeting filler.** Cards 2–4 re-introduce Meera or echo the welcome card.
   FIX: Card 2 starts the teaching immediately. The welcome card already introduced everyone.

You may rewrite card prose freely. Do NOT add or remove cards (the plan's card_plan is the spec — its slot count is the dialogue's length). If two adjacent cards have the same speaker because the plan demands it (e.g. tutor `student-act` instruction → tutor `articulate` later), keep both but make the prose feel like one continuous beat with a peer turn between when possible — only restructure if the plan slot allows it. Re-number `card_idx` consecutively starting at 2 to match the plan's `slot` values.

═══════════════════════════════════════════════════════════════════
TONE
═══════════════════════════════════════════════════════════════════

- Mr. Verma: warm, patient, simple words. Treats Meera and the real student kindly. Never patronizing, never babyish.
- Meera: curious, friendly, not-quite-there-yet. Confident enough to guess aloud. Vocabulary slightly simpler than the tutor's.

OUTPUT FORMAT — same JSON schema as the generation pass. Respond with valid JSON only — no markdown fences, no commentary.

{
  "$defs": {
    "DialogueLineOutput": {
      "type": "object",
      "additionalProperties": false,
      "required": ["display", "audio"],
      "properties": {
        "display": {"type": "string"},
        "audio":   {"type": "string"}
      }
    },
    "CheckInActivityOutput": {
      "type": "object",
      "additionalProperties": false,
      "required": ["activity_type", "instruction", "hint", "success_message", "audio_text"],
      "properties": {
        "activity_type":   {"type": "string"},
        "instruction":     {"type": "string"},
        "hint":            {"type": "string"},
        "success_message": {"type": "string"},
        "audio_text":      {"type": "string"},
        "options":         {"anyOf": [{"type": "array", "items": {"type": "string"}}, {"type": "null"}]},
        "correct_index":   {"anyOf": [{"type": "integer"}, {"type": "null"}]},
        "statement":       {"anyOf": [{"type": "string"},  {"type": "null"}]},
        "correct_answer":  {"anyOf": [{"type": "boolean"}, {"type": "null"}]},
        "pairs":           {"anyOf": [{"type": "array", "items": {"type": "object", "required": ["left","right"], "additionalProperties": false, "properties": {"left":{"type":"string"}, "right":{"type":"string"}}}}, {"type": "null"}]},
        "bucket_names":    {"anyOf": [{"type": "array", "items": {"type": "string"}}, {"type": "null"}]},
        "bucket_items":    {"anyOf": [{"type": "array", "items": {"type": "object", "required": ["text","correct_bucket"], "additionalProperties": false, "properties": {"text":{"type":"string"}, "correct_bucket":{"type":"integer"}}}}, {"type": "null"}]},
        "sequence_items":  {"anyOf": [{"type": "array", "items": {"type": "string"}}, {"type": "null"}]},
        "error_steps":     {"anyOf": [{"type": "array", "items": {"type": "string"}}, {"type": "null"}]},
        "error_index":     {"anyOf": [{"type": "integer"}, {"type": "null"}]},
        "odd_items":       {"anyOf": [{"type": "array", "items": {"type": "string"}}, {"type": "null"}]},
        "odd_index":       {"anyOf": [{"type": "integer"}, {"type": "null"}]},
        "reveal_text":     {"anyOf": [{"type": "string"},  {"type": "null"}]}
      }
    },
    "DialogueCardOutput": {
      "type": "object",
      "additionalProperties": false,
      "required": ["card_idx", "card_type", "speaker", "speaker_name", "title", "lines", "includes_student_name", "visual_intent", "check_in"],
      "properties": {
        "card_idx":              {"type": "integer"},
        "card_type":             {"type": "string", "enum": ["tutor_turn", "peer_turn", "visual", "check_in", "summary"]},
        "speaker":               {"anyOf": [{"type": "string", "enum": ["tutor", "peer"]}, {"type": "null"}]},
        "speaker_name":          {"anyOf": [{"type": "string"}, {"type": "null"}]},
        "title":                 {"anyOf": [{"type": "string"}, {"type": "null"}]},
        "lines":                 {"type": "array", "items": {"$ref": "#/$defs/DialogueLineOutput"}},
        "includes_student_name": {"type": "boolean"},
        "visual_intent":         {"anyOf": [{"type": "string"}, {"type": "null"}]},
        "check_in":              {"anyOf": [{"$ref": "#/$defs/CheckInActivityOutput"}, {"type": "null"}]}
      }
    }
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["cards"],
  "properties": {
    "cards": {"type": "array", "items": {"$ref": "#/$defs/DialogueCardOutput"}}
  }
}
